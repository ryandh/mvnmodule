one project with two modules
module CSharpClient can refer ModuleCSharp which is under the same parent

mvn install //install pom and moudles
go to cSharpClient
mvn assemlbly:single will compile a runnable jar
